# Squirrel
- Built features using K-means clustering and KNN smoothing on spatial data (OOS to prevent data leakage)  
- Built a stacked ensemble using K-Fold CV to create OOF predictions from logistic regression and decision tree classifiers for meta-model training, achieving 96% ROC-AUC (+26% vs baseline RF w/o feat. eng.)  
